{
  "name": "naruto-blog-railway",
  "version": "1.0.0",
  "description": "Naruto Blog Server for Railway",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "echo 'No build step required'",
    "railway-start": "cd server && npm install && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}